# 游踪 GeoTrack 需求文档（简化落地版）

## 1. 项目概述

### 1.1 项目名称
**游踪 GeoTrack —— 基于 LBS 的文旅打卡与积分激励平台**

### 1.2 项目定位
面向文旅打卡场景，提供“到点打卡、图片上传、内容发布、积分奖励、积分兑换、限量秒杀”等核心能力。项目以高并发后端能力为重点，突出 LBS 校验、异步解耦、秒杀防超卖、最终一致性等技术方案。

### 1.3 建设目标
1. 支持用户在指定 POI（兴趣点）完成地理位置打卡。
2. 支持图片直传对象存储，减少后端带宽压力。
3. 用户打卡后自动生成动态并进入 POI 圈子。
4. 支持积分奖励、积分商城兑换和限量秒杀。
5. 具备基本的限流、防刷、幂等、消息异步处理能力。

### 1.4 项目边界
本版本仅实现可落地的核心闭环，不做复杂推荐系统、复杂社交关系链、分库分表和大规模搜索引擎优化。

---

## 2. 用户角色

### 2.1 普通用户
- 浏览 POI 列表
- 完成打卡
- 上传图片
- 发布动态
- 点赞、评论
- 参与积分兑换和秒杀

### 2.2 管理员
- 创建和管理 POI 打卡点
- 配置打卡范围、奖励积分、任务状态
- 上架积分商品与秒杀商品
- 查看打卡记录、订单记录与基础运营数据

---

## 3. 业务流程概述

### 3.1 打卡流程
1. 管理员创建 POI 打卡任务。
2. 用户进入 POI 附近后发起打卡请求。
3. 后端校验用户 GPS 坐标是否在打卡半径内。
4. 用户上传图片至对象存储。
5. 打卡成功后发放积分，并生成一条动态。

### 3.2 兑换/秒杀流程
1. 用户进入积分商城。
2. 选择商品后发起兑换或秒杀请求。
3. 系统校验库存、积分与用户资格。
4. 扣减库存并生成订单。
5. 订单成功后扣减积分；失败则回滚或补偿。

### 3.3 动态与互动流程
1. 用户打卡成功后生成动态。
2. 动态自动归属到对应 POI 圈子。
3. 其他用户可进行点赞、评论、浏览热门内容。

---

## 4. 功能需求

## 4.1 用户与认证模块

### 4.1.1 注册/登录
- 支持手机号验证码登录或账号密码登录。
- 登录成功后返回 token。
- 支持 token 刷新与退出登录。

### 4.1.2 用户基础信息
- 用户可维护昵称、头像、简介。
- 系统记录用户积分余额与打卡次数。

---

## 4.2 POI 打卡模块

### 4.2.1 POI 创建与管理
管理员可配置以下信息：
- POI 名称
- 经纬度坐标
- 打卡半径（如 500 米）
- 任务描述
- 奖励积分
- 状态（启用/停用）

### 4.2.2 打卡校验
用户发起打卡时，系统应校验：
- 用户当前 GPS 坐标是否在 POI 范围内。
- 用户是否已完成该 POI 当日打卡。
- 用户是否满足基础风控条件。

### 4.2.3 打卡结果
- 打卡成功：记录打卡流水、发放积分、生成动态。
- 打卡失败：返回失败原因，如“未在打卡范围内”“今日已打卡”“请求过于频繁”。

### 4.2.4 重复打卡控制
- 同一用户对同一 POI 同一自然日只能成功一次。
- 重复请求应具备幂等性。

---

## 4.3 图片上传与内容处理模块

### 4.3.1 图片上传
- 前端通过后端获取签名或临时凭证后，直接上传至 MinIO/OSS。
- 后端不承载大文件传输流量。

### 4.3.2 图片处理
- 上传成功后生成图片访问地址。
- 后端异步进行图片压缩、缩略图生成、基础内容审核。
- 处理结果回写数据库。

### 4.3.3 图片去重
- 系统可根据图片 MD5 做基础去重判断。
- 防止同一图片重复提交。

---

## 4.4 动态与 POI 圈子模块

### 4.4.1 动态发布
- 用户打卡成功后自动生成一条动态。
- 动态内容包含：用户信息、POI 信息、图片、发布时间、积分奖励说明。

### 4.4.2 POI 圈子
- 每个 POI 对应一个独立圈子。
- 用户可按 POI 查看圈子内动态。
- 支持按时间、热度查看列表。

### 4.4.3 互动功能
- 支持点赞。
- 支持评论。
- 支持查看动态详情。

### 4.4.4 热门榜单
- 系统按点赞数、评论数与时间衰减计算热度值。
- 每个 POI 圈子可展示热门动态榜单。

---

## 4.5 积分与商城模块

### 4.5.1 积分获取
用户可通过以下行为获得积分：
- 完成 POI 打卡
- 发布内容被判定为有效
- 完成特定活动任务

### 4.5.2 积分流水
- 每次积分变动必须记录流水。
- 支持查询积分明细。

### 4.5.3 商品兑换
- 用户可使用积分兑换商品。
- 商品分为普通兑换商品和限量秒杀商品。

### 4.5.4 库存与订单
- 兑换成功后生成订单。
- 库存扣减必须避免超卖。
- 下单失败需支持回滚或补偿。

---

## 4.6 秒杀模块

### 4.6.1 秒杀场景
- 运营可配置限量秒杀商品。
- 商品在活动时间内开放抢购。

### 4.6.2 秒杀规则
- 单用户限购一次或按规则限购。
- 库存不足时直接返回失败。
- 系统应能承受短时高并发请求。

### 4.6.3 防超卖
- 使用 Redis Lua 脚本进行库存原子扣减。
- 订单落库与库存扣减结果保持一致。

---

## 4.7 风控与限流模块

### 4.7.1 接口限流
- 对打卡接口、秒杀接口、点赞接口进行限流。
- 支持按用户、按 IP、按接口维度控制频率。

### 4.7.2 幂等控制
- 打卡、秒杀、积分扣减等核心接口必须支持幂等。

### 4.7.3 防刷策略
- 短时间频繁请求直接拦截。
- 对异常用户可做黑名单处理。

---

## 5. 非功能需求

### 5.1 性能要求
- 打卡校验接口应支持高并发访问。
- 秒杀接口在短时流量峰值下不应出现库存超卖。
- 图片上传不应阻塞核心业务线程。

### 5.2 可用性要求
- 核心链路应支持消息重试与失败补偿。
- 核心数据应具备基础降级策略。

### 5.3 一致性要求
- 打卡成功后积分发放应最终一致。
- 秒杀订单、库存、积分扣减应保持业务一致。

### 5.4 可维护性要求
- 服务拆分清晰，职责单一。
- 关键流程需有日志、异常和追踪信息。

---

## 6. 技术实现建议

### 6.1 推荐技术栈
- Spring Boot
- Spring Cloud Alibaba
- MySQL
- Redis
- RocketMQ
- MinIO / OSS
- Sentinel
- Nacos
- MyBatis-Plus

### 6.2 核心技术方案
- Redis GEO：POI 附近范围查询与打卡校验
- Redis Lua：秒杀库存原子扣减
- RocketMQ：异步解耦、图片处理、积分发放、订单消息
- Sentinel：限流与降级
- MinIO/OSS：图片直传与存储

---

## 7. 数据对象设计

### 7.1 用户表
记录用户基本信息、积分余额、状态等。

### 7.2 POI 表
记录打卡点名称、经纬度、半径、奖励积分、状态等。

### 7.3 打卡记录表
记录用户、POI、打卡时间、坐标、结果、图片地址等。

### 7.4 动态表
记录动态内容、作者、POI 归属、热度值、发布时间等。

### 7.5 评论表
记录评论内容、评论人、所属动态、时间等。

### 7.6 商品表
记录商品名称、积分价格、库存、状态、活动信息等。

### 7.7 订单表
记录兑换订单、下单用户、商品、状态、支付/扣减结果等。

### 7.8 积分流水表
记录积分变更前后值、变更原因、关联业务单据等。

---

## 8. 核心接口清单

### 8.1 用户类接口
- 登录
- 获取用户信息
- 更新用户资料

### 8.2 POI 类接口
- 创建 POI
- 查询 POI 列表
- POI 详情
- 用户打卡

### 8.3 动态类接口
- 发布动态
- 查询动态列表
- 查询动态详情
- 点赞
- 评论

### 8.4 商城类接口
- 查询商品列表
- 商品详情
- 发起兑换
- 发起秒杀
- 查询订单

### 8.5 运维类接口
- 查询打卡记录
- 查询积分流水
- 查询订单状态

---

## 9. 关键业务规则

1. 同一用户同一 POI 同一天仅允许成功打卡一次。
2. 打卡成功后才能发放积分。
3. 积分扣减必须以订单成功为前提。
4. 秒杀库存扣减必须原子化，禁止超卖。
5. 核心业务接口必须具备幂等能力。
6. 图片上传与内容处理必须异步化，避免阻塞主链路。

---

## 10. 验收标准

### 10.1 功能验收
- 用户可完成登录、打卡、上传、发动态、点赞评论、兑换商品。
- 管理员可创建 POI 和商品。
- 秒杀场景下不出现超卖。

### 10.2 技术验收
- 打卡校验链路可通过 Redis GEO 完成。
- 图片上传采用直传方式，不经过后端大流量转发。
- 秒杀与订单具备最终一致性方案。

### 10.3 展示验收
- 可提供项目架构图。
- 可提供核心接口文档。
- 可提供压测或模拟并发结果。
- 可提供关键代码说明与问题复盘。

---

## 11. 里程碑建议

### 第一阶段：基础闭环
- 用户登录
- POI 管理
- 打卡校验
- 图片上传
- 积分发放

### 第二阶段：高并发能力
- Redis GEO
- RocketMQ 异步处理
- 秒杀库存控制
- 最终一致性

### 第三阶段：展示增强
- 动态流
- 点赞评论
- 热门榜单
- 限流防刷

### 第四阶段：项目包装
- 接口文档
- 架构图
- 压测截图
- 简历描述
- 面试题总结

---

## 12. 项目结论

本项目以文旅打卡为业务背景，以 Java 后端高并发能力为核心展示目标。通过保留“打卡校验、图片直传、积分激励、秒杀兑换、动态互动”五个关键模块，可以在可控开发周期内完成一个**适合简历展示、适合面试讲解、适合实际落地**的后端项目。

